const request = require('supertest');

const router = require('./shopping-router.js');

describe('shopping-router.js', () => {

  describe('GET /shopping', () => {

    it('Should return status code 200', () => {
      // Stuff
    });

    it('Should return an array', () => {
      // Stuff
    })

  });

  describe('GET /shopping/:id', () => {

    it('Should return status code 200', () => {
      // Stuff
    });

    it('Should return an array', () => {
      // Stuff
    })
    
  });

  describe('POST /shopping', () => {

    it('Should return status code 200', () => {
      // Stuff
    });

    it('Should return an array', () => {
      // Stuff
    })
    
  });

  describe('PUT /shopping/:id', () => {

    it('Should return status code 200', () => {
      // Stuff
    });

    it('Should return an array', () => {
      // Stuff
    })
    
  });

  describe('DELETE /shopping/:id', () => {

    it('Should return status code 200', () => {
      // Stuff
    });

    it('Should return an array', () => {
      // Stuff
    })
    
  });

});